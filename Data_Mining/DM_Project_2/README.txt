Implemented in Python 2.7 using Sublime Text 2

all implementation is in main.py

to run, specify the file path for transactions and products in main(), then build!

the min support and confidence for frequent item set generation can also be specified in main()
